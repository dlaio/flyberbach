<% provide(:title, 'All users') %>
<div class="row">
  <div class="span6 offset5">
    <h1>All Users</h1>
  </div>
</div>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>