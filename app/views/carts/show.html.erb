<% provide(:title, 'Shopping Cart') %>
<div class="row">
  <div class="span6 offset5">
    <h1>Shopping Cart</h1>
  </div>
</div>

<div class="span10 offset1">
    <table class="table table-striped table-hover"  width = "800">
        <tr>
            <th>Airline</th>
            <th>FlightNo</th>
            <th>PlaneType</th>
            <th>Departing Time</th>
            <th>Departing Airport</th>
            <th>Arrival Time</th>
            <th>Arriving Airport</th>
            <th>Price</th>
            <th></th>
        </tr>

		<% for item in @cart.line_items %>
			<%#<li><%= item.flight.airline %>
			<%#<%= render :partial => @item.flight %>
			<tr>
			    <td><%= item.flight.airline %></td>
			    <td><%= item.flight.flight_no %></td>
			    <td><%= item.flight.plane_type %></td>
			    <td><%= item.flight.leave_time %></td>
			    <td><%= item.flight.leave_city %></td>
			    <td><%= item.flight.arrive_time %></td>
			    <td><%= item.flight.arrive_city %></td>
			    <td><%= item.flight.price %></td> 
			    <td><%= button_to 'Remove', '#' %>
			</tr>
		<%end%>



        <%#<%= will_paginate %>
        <%#<ul class="flights">
        <%#    <%= render @flights %>
        <%#</ul>
        <%= will_paginate %>
     </table>

    	Cart total: <%= number_to_currency(@cart.total_price) %>
    	<%= button_to 'Cancel Order', '#' %><%= button_to 'Checkout', '#' %>
  	
</div>


